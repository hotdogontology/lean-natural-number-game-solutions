intro h,
rw succ_eq_add_one at h,
cases h with k h,
rw add_assoc at h,
symmetry at h,
have h₁ := eq_zero_of_add_right_eq_self(h),
rw add_comm at h₁,
rw ← succ_eq_add_one at h₁,
have h₂ := succ_ne_zero(k),
apply h₂,
exact h₁,
